"""
format module
=============
Package: `src`

Module to/that # TODO: set docstring

Functions
---------
- `formatData`
"""

from . import models


def formatData(
        games: list[models.Game],
        notes: list[models.Note],
        editors: list[models.Editor],
        stocks: list[models.Stock],
        /
        ) -> list[models.Data]:
    """
    Method to/that # TODO: set docstring

    Args:
        games (list[models.Game]): list of games.
        notes (list[models.Note]): list of notes.
        editors (list[models.Editor]): list of editors.
        stocks (list[models.Stock]): list of stocks.
    Raises:
        # TODO: set exceptions
    """
    data_list: list[models.Data] = []

    # TODO: implement function

    return data_list
